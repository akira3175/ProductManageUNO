<UserControl x:Class="ProductManageUNO.Presentation.Shell"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:ProductManageUNO.Presentation"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:utu="using:Uno.Toolkit.UI"
      mc:Ignorable="d"
      d:DesignHeight="300"
      d:DesignWidth="400">
  <Border Background="{ThemeResource PrimaryBrush}">
    <utu:ExtendedSplashScreen x:Name="Splash"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                VerticalContentAlignment="Stretch">
      <utu:ExtendedSplashScreen.LoadingContentTemplate>
        <DataTemplate>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <!-- App Logo -->
            <StackPanel Grid.Row="1" 
                        HorizontalAlignment="Center" 
                        VerticalAlignment="Center"
                        Spacing="20">
              <!-- Icon Circle -->
              <Border Background="#FFFFFF"
                      CornerRadius="40"
                      Width="120"
                      Height="120"
                      HorizontalAlignment="Center"
                      Opacity="0.95">
                <Border Background="#5946D2"
                        CornerRadius="32"
                        Width="100"
                        Height="100"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                  <FontIcon Glyph="&#xE7BF;" 
                           FontSize="48" 
                           Foreground="White"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                </Border>
              </Border>
              
              <!-- App Name -->
              <TextBlock Text="Product Manager"
                        FontSize="28"
                        FontWeight="Bold"
                        Foreground="White"
                        HorizontalAlignment="Center"
                        CharacterSpacing="40"/>
              
              <TextBlock Text="Quản lý bán hàng thông minh"
                        FontSize="14"
                        Foreground="White"
                        Opacity="0.8"
                        HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Loading Indicator -->
            <StackPanel Grid.Row="2" 
                        HorizontalAlignment="Center"
                        Margin="0,48,0,0"
                        Spacing="12">
              <ProgressRing IsActive="True"
                           Height="40"
                           Width="40"
                           Foreground="White"/>
              <TextBlock Text="Đang tải..."
                        FontSize="13"
                        Foreground="White"
                        Opacity="0.7"
                        HorizontalAlignment="Center"/>
            </StackPanel>
          </Grid>
        </DataTemplate>
      </utu:ExtendedSplashScreen.LoadingContentTemplate>
    </utu:ExtendedSplashScreen>
  </Border>
</UserControl>
