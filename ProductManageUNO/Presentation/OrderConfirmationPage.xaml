<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="ProductManageUNO.Presentation.OrderConfirmationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ProductManageUNO.Presentation"
    Background="{ThemeResource BackgroundBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" 
                   VerticalAlignment="Center"
                   Spacing="28"
                   Padding="36"
                   MaxWidth="400">
            
            <!-- Success Icon with animated-like appearance -->
            <Border Background="#E8F5E9"
                    CornerRadius="60"
                    Width="120"
                    Height="120"
                    HorizontalAlignment="Center">
                <Border Background="#C8E6C9"
                        CornerRadius="45"
                        Width="90"
                        Height="90"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE73E;" 
                             FontSize="48" 
                             Foreground="#2E7D32"
                             HorizontalAlignment="Center"
                             VerticalAlignment="Center"/>
                </Border>
            </Border>

            <!-- Success Message -->
            <StackPanel Spacing="8">
                <TextBlock Text="Đặt hàng thành công!"
                          FontSize="28"
                          FontWeight="Bold"
                          Foreground="{ThemeResource OnSurfaceBrush}"
                          HorizontalAlignment="Center"
                          CharacterSpacing="20"/>
                <TextBlock Text="Đơn hàng của bạn đã được ghi nhận"
                          FontSize="14"
                          Foreground="{ThemeResource OnSurfaceVariantBrush}"
                          HorizontalAlignment="Center"
                          Opacity="0.8"/>
            </StackPanel>

            <!-- Order Details Card -->
            <Border Background="{ThemeResource SurfaceBrush}"
                    CornerRadius="16"
                    Padding="24"
                    BorderBrush="{ThemeResource OutlineVariantBrush}"
                    BorderThickness="1">
                <StackPanel Spacing="18">
                    <!-- Order ID -->
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xE7C3;" FontSize="16" Foreground="{ThemeResource OnSurfaceVariantBrush}"/>
                            <TextBlock Text="Mã đơn hàng"
                                      FontSize="14"
                                      Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                        <Border Grid.Column="1" 
                                Background="{ThemeResource PrimaryContainerBrush}"
                                CornerRadius="8"
                                Padding="12,6">
                            <TextBlock x:Name="OrderIdText"
                                      FontSize="14"
                                      FontWeight="Bold"
                                      Foreground="{ThemeResource PrimaryBrush}"/>
                        </Border>
                    </Grid>
                    
                    <!-- Customer Name -->
                    <Grid ColumnDefinitions="*,Auto">
                        <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10">
                            <FontIcon Glyph="&#xE77B;" FontSize="16" Foreground="{ThemeResource OnSurfaceVariantBrush}"/>
                            <TextBlock Text="Khách hàng"
                                      FontSize="14"
                                      Foreground="{ThemeResource OnSurfaceVariantBrush}"
                                      VerticalAlignment="Center"/>
                        </StackPanel>
                        <TextBlock x:Name="CustomerNameText"
                                  Grid.Column="1"
                                  FontSize="15"
                                  FontWeight="SemiBold"
                                  Foreground="{ThemeResource OnSurfaceBrush}"/>
                    </Grid>

                    <Border Height="1" Background="{ThemeResource OutlineVariantBrush}"/>

                    <!-- Total Amount -->
                    <Grid ColumnDefinitions="*,Auto">
                        <TextBlock Grid.Column="0"
                                  Text="Tổng tiền"
                                  FontSize="16"
                                  FontWeight="SemiBold"
                                  Foreground="{ThemeResource OnSurfaceBrush}"
                                  VerticalAlignment="Center"/>
                        <TextBlock x:Name="TotalAmountText"
                                  Grid.Column="1"
                                  FontSize="24"
                                  FontWeight="Bold"
                                  Foreground="{ThemeResource PrimaryBrush}"/>
                    </Grid>
                </StackPanel>
            </Border>

            <!-- Thank You Message -->
            <Border Background="{ThemeResource SurfaceVariantBrush}"
                    CornerRadius="10"
                    Padding="16,12"
                    HorizontalAlignment="Stretch">
                <TextBlock Text="🎉 Cảm ơn bạn đã mua hàng!"
                          FontSize="15"
                          Foreground="{ThemeResource OnSurfaceVariantBrush}"
                          HorizontalAlignment="Center"/>
            </Border>

            <!-- Buttons -->
            <Grid ColumnDefinitions="*,*" ColumnSpacing="12" Margin="0,8,0,0">
                <Button Grid.Column="0"
                       x:Name="ViewOrdersButton"
                       Click="ViewOrdersButton_Click"
                       HorizontalAlignment="Stretch"
                       Background="Transparent"
                       BorderBrush="{ThemeResource PrimaryBrush}"
                       BorderThickness="2"
                       Padding="16,14"
                       CornerRadius="12">
                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE7C3;" FontSize="18" Foreground="{ThemeResource PrimaryBrush}"/>
                        <TextBlock Text="Xem đơn" FontSize="15" FontWeight="SemiBold" Foreground="{ThemeResource PrimaryBrush}"/>
                    </StackPanel>
                </Button>
                
                <Button Grid.Column="1"
                       x:Name="BackToHomeButton"
                       Click="BackToHomeButton_Click"
                       HorizontalAlignment="Stretch"
                       Style="{ThemeResource AccentButtonStyle}"
                       Padding="16,14"
                       CornerRadius="12">
                    <StackPanel Orientation="Horizontal" Spacing="10" HorizontalAlignment="Center">
                        <FontIcon Glyph="&#xE80F;" FontSize="18"/>
                        <TextBlock Text="Trang chủ" FontSize="15" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </Grid>
        </StackPanel>
    </Grid>
</Page>
